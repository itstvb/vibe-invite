export default function AdminTemplatesPage() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>Admin Templates</h1>
      <p>
        Admin template metadata and schema changes must be guarded by role checks and written with the Admin SDK.
      </p>
    </main>
  );
}
