/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@vibeinvite/config', '@vibeinvite/templates', '@vibeinvite/types', '@vibeinvite/ui']
};

export default nextConfig;
