export default function InvitationIndexPage() {
  return (
    <main style={{ display: 'grid', minHeight: '100vh', placeItems: 'center', padding: '2rem' }}>
      <p>Open a published invitation at /i/[slug].</p>
    </main>
  );
}
